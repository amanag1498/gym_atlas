import 'package:flutter/material.dart';

import '../trainer_app.dart';

class LegacyTrainerApp extends StatelessWidget {
  const LegacyTrainerApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const TrainerApp();
  }
}
