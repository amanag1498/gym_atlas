import 'package:flutter/material.dart';

import '../member_app.dart';

class LegacyMemberApp extends StatelessWidget {
  const LegacyMemberApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MemberApp();
  }
}
